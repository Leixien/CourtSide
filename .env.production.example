# Production Environment Variables for Vercel Deployment

# MongoDB Atlas (Free tier: https://www.mongodb.com/cloud/atlas/register)
# 1. Create free cluster
# 2. Create database user
# 3. Whitelist all IPs (0.0.0.0/0) for serverless
# 4. Get connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/courtside?retryWrites=true&w=majority

# JWT Secret - Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-production-secret

# OAuth Providers (Optional but recommended)
GITHUB_ID=your-github-oauth-id
GITHUB_SECRET=your-github-oauth-secret
GOOGLE_ID=your-google-oauth-id
GOOGLE_SECRET=your-google-oauth-secret

# Sports API
SPORTS_API_KEY=your-api-key
SPORTS_API_URL=https://api-football-v1.p.rapidapi.com/v3

# Push Notifications - Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=mailto:your-email@example.com

# App URL
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
NEXT_PUBLIC_SOCKET_URL=https://your-app.vercel.app

NODE_ENV=production
